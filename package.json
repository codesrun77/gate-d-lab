{
  "name": "gate-d-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Sandbox repository for Gate D. Deliberately carries one real bug.",
  "type": "module",
  "engines": { "node": ">=20.11" },
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
